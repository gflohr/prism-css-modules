# Prism-CSS-Modules

Show how to use PrismJS with css-modules.  This includes display of
line numbers.

## Usage

With NPM:

```shell
$ npm install
$ npm start
```

With Yarn:

```shell
$ yarn install
$ yarn start
```
